from PIL import Image
import io
import base64

def sd_inpaint(img, mask, prompt, negative_prompt, steps, denoising, cfg_scale, model, preprocessor):
    """
    Placeholder funkcja dla inpaintingu z SD + ControlNet.
    Na razie zwraca kopię obrazu (brak realnej implementacji).
    """
    return img.copy()
